<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
<h1>Create a New Figure</h1>
	<form action="/figures" method="POST">

		Figure Name: <input type="text" name="figure[name]" id="figure_name"><br><br>
		New Title: <input type="text" name="title[name]" id="new_title"><br>
		<label>Optional Titles for this Figure: 
		<% Title.all.each do |title| %> <br>
			<input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%= title.id %>"><%= title.name %></input>
		<% end %><br><br>
		</label>                             
		New Landmark: <input type="text" name="landmark[name]" id="new_landmark"><br>
		<label>Optional Landmarks for this Figure:
		<% Landmark.all.each do |landmark| %> <br>
			<input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%= landmark.id %>"><%=landmark.name%></input>
		<% end %><br>
		</label>
		<br>
		<input type="submit" value="Create New Figure">
		
	</form>
</body>
</html>